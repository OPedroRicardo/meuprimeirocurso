/*
Proposta:
  1. Fazer uma função que vai executar o seguinte expressão (x vezes y) dividido por w
  2. Usar 3 funções do prototype da string
*/

function myFunction (x, y, w) {
  var expressao = (x * y) / w;

  console.log(expressao);
}

myFunction(2, 4, 2)

var texto = 'Olá mundo. Tudo bem?'

console.log(texto.toLowerCase());
console.log(texto.length);
console.log(texto.substring(0, 10));
